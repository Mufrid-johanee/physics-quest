# Zone 02 mini-game scripts

**Status: UNUSED LEGACY** — not launched. Active Zone 02 gate is the Floor4 PLAY → SUCCESSFUL placeholder in `scripts/zone02/Zone02Floor4.gd`.

Controller: `MiniGame02HarborWorks.gd` — extends `Control` (no `MiniGameBase`).

Verifier (legacy scene only): `res://scripts/tools/verify_mg02_harbor_works.gd`  
Living docs: `Docs/MINIGAME_IMPLEMENTATION.md`
